export enum AppRoute {
  Catalog = '/',
  Product = '/cameras/:id',
  Basket = '/orders'
}

export enum NameSpace {
  Products = 'PRODUCTS',
  Banner = 'BANNER'
}

export enum APIRoute {
  Banner = '/promo',
  Products = '/cameras'
}
